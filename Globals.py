# Stuff that doesn't really fit anywhere else goes here. An interesting programming question:
# "Should there always be a junk drawer, or never be a junk drawer?"
# Anyway, this is a junk drawer

class Globals():
    NUM_OF_PEGS = 4
    PEG_IND_LIST = list(range(0, NUM_OF_PEGS, 1))

    NUM_OF_GUESSES = 10
    GUESS_IND_LIST = list(range(1, 11, 1))